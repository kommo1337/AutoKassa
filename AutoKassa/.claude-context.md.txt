# AutoKassa - Личная Касса

## Техническое Задание

### Описание проекта
Desktop приложение для учета личных финансов с возможностью:
- Записи доходов и расходов
- Категоризации операций
- Формирования отчетов
- Анализа финансов
- Планирования бюджета
- Защиты данных паролем

### Целевая аудитория
Физические лица, желающие вести учет личных финансов на локальном компьютере без облачных сервисов.

### Основные требования
- Локальное хранение данных (SQLite)
- Защита паролем
- Простой и понятный интерфейс
- Быстрая работа
- Наглядная визуализация (графики, диаграммы)
- Экспорт отчетов

---

## Технологический стек

- **.NET 8.0** - платформа
- **WPF** - UI фреймворк
- **Entity Framework Core 8.0** - ORM
- **SQLite** - база данных
- **MVVM** - архитектурный паттерн
- **Dependency Injection** - управление зависимостями
- **OxyPlot 2.2.0** - графики и диаграммы

---

## Структура промтов (этапы разработки)

### ✅ ЭТАП 1: ФУНДАМЕНТ (ВЫПОЛНЕНО)

**Промт 1.1: Инициализация проекта**
- Создание WPF проекта
- Настройка .NET 8.0
- Установка NuGet пакетов (EF Core, DI)

**Промт 1.2: Модели данных**
- `Transaction` - операции
- `Category` - категории
- `Settings` - настройки
- Enum `OperationType`

**Промт 1.3: База данных**
- `AppDbContext`
- Создание миграций
- Seed данных (категории по умолчанию)

**Промт 1.4: Базовая архитектура**
- `ViewModelBase` - базовый класс VM
- `RelayCommand` - реализация ICommand
- Настройка DI контейнера в `App.xaml.cs`

**Промт 1.5: Стили и темы**
- `Colors.xaml` - цветовая палитра
- `Buttons.xaml` - стили кнопок
- Глобальные ресурсы

---

### ✅ ЭТАП 2: ОСНОВНОЙ ФУНКЦИОНАЛ (ВЫПОЛНЕНО)

**Промт 2.1: Начальная настройка**
- `InitialSetupViewModel` - первый запуск
- `InitialSetupView` - окно настройки
- Установка пароля
- Создание БД

**Промт 2.2: Главное окно**
- `MainWindowViewModel` - навигация
- `MainWindow.xaml` - боковое меню
- Навигация между экранами

**Промт 2.3: Блокировка экрана**
- `LockService` - управление блокировкой
- `PasswordService` - хеширование паролей
- `LockScreenView` - окно ввода пароля
- Автоблокировка по неактивности

**Промт 2.4: Управление операциями**
- `TransactionService` - CRUD операций
- `TransactionsViewModel` - список операций
- `TransactionsView` - таблица с фильтрацией
- `TransactionEditViewModel` - добавление/редактирование
- `TransactionEditView` - форма операции

**Промт 2.5: Управление категориями**
- `CategoryService` - CRUD категорий
- `CategoriesViewModel` - список категорий
- `CategoriesView` - карточки категорий
- `CategoryEditViewModel` - редактор категории
- `CategoryEditView` - форма с ColorPicker

---

### ✅ ЭТАП 3: ОТЧЕТЫ (В ПРОЦЕССЕ)

**Промт 3.1: Структура отчетов**
- `ReportBase` - базовая модель отчета
- `IReportService` - интерфейс сервиса
- `ReportService` - реализация
- `BaseReportViewModel` - базовая VM для отчетов
- `ReportsViewModel` - главный экран отчетов
- `ReportsView` - боковая панель с типами отчетов

**Промт 3.2: Отчет "Баланс за период"**
- ✅ `BalanceReport` - модель отчета
- ✅ Метод в `ReportService`
- ✅ `BalanceReportViewModel` - VM отчета
- ✅ `BalanceReportView` - UI отчета
- ✅ Быстрые фильтры периодов
- ✅ Сводка (начальный/конечный баланс, доходы, расходы)
- ✅ График OxyPlot (столбцы доходов/расходов + линия баланса)

**Промт 3.3: График для отчета баланса (ВЫПОЛНЕНО)**
- ✅ Установка библиотеки OxyPlot 2.2.0
- ✅ Обновление ViewModel для данных графика
- ✅ Обновление View с компонентом графика
- ✅ Столбцы доходов/расходов + линия баланса

**Промт 3.4: Отчет "Структура по категориям" (планируется)**
- Круговая диаграмма расходов
- Группировка по категориям
- Процентное соотношение

    
Параметры:

Дата от (DateTime)
Дата до (DateTime)
Тип: Доходы / Расходы / Оба (при выборе "Оба" отображаются две диаграммы рядом)

Расчетные показатели:

Сумма по каждой категории
Доля категории в общей сумме (%)
Количество операций по категории

Визуализация:

Круговая диаграмма (Pie Chart) с сегментами по категориям
Цветовая дифференциация сегментов (автоматическая палитра)
Подписи: название категории и процент
Tooltip: название, сумма (₽), процент, количество операций
При выборе "Оба": две диаграммы рядом (слева — доходы, справа — расходы)

Табличное представление:
Колонки: Категория | Сумма (₽) | Доля (%) | Количество операций

Сортировка по убыванию суммы
Итоговая строка с общей суммой

Создай отчет "Структура по категориям" согласно ТЗ раздел 3.3:

Views/CategoryStructureReportView.xaml:
- Панель параметров: DatePicker (от/до), Radio buttons (Доходы/Расходы/Оба)
- График: круговая диаграмма (LiveCharts2 PieChart), tooltips, легенда
- Если выбрано "Оба": две диаграммы рядом
- Checkbox "Скрыть категории < 1%"
- Таблица: колонки (Категория, Сумма, Доля%, Количество операций), итоговая строка
- Кнопки экспорта

ViewModels/CategoryStructureReportViewModel.cs:
- Параметры: DateFrom, DateTo, Type
- Загрузка данных по категориям
- Расчет долей в процентах
- Фильтрация малых категорий

Services/CategoryStructureReportService.cs:
- GetCategoryData(dateFrom, dateTo, type) → List<CategoryData> {Category, Amount, Share%, OperationCount}


**Промт 3.6: Экспорт отчетов (планируется)**
- PDF (QuestPDF)
- Excel (EPPlus/ClosedXML)
- Печать отчетов
Создай функциональность экспорта отчетов в PDF с использованием QuestPDF
Services/PdfExportService.cs:

Методы для каждого типа отчета: ExportBalanceReport(), ExportCategoryStructureReport() и т.д.
Поддержка трех шаблонов:

Полный (график + таблица на отдельных страницах)
Компактный (график и таблица на одной странице)
Только график


Титульный лист с параметрами отчета
График как изображение (PNG, 300 DPI) 
Таблица с форматированием
Стилизация согласно цветовой схеме приложения

Helpers/ChartToImageConverter.cs:

Метод ConvertChartToImage(Chart chart) → byte[]
Сохранение графика как PNG

Установи NuGet: QuestPDF

Создай функциональность экспорта отчетов в Excel с использованием EPPlus:
Services/ExcelExportService.cs:

Методы для каждого типа отчета
Два лист: "Данные"

Таблица с форматированием (шапка выделена, итоговые строки жирным)
Стилизация ячеек (цвета, границы)
---




### 📋 ЭТАП 6: УЛУЧШЕНИЯ (ПЛАНИРУЕТСЯ)

**Промт 6.1: Темная тема**
- Переключатель темы
- Dark Colors.xaml
- Сохранение выбора темы

**Промт 6.2: Анимации**
- Плавные переходы
- Анимация загрузки
- Fade-эффекты

**Промт 6.3: Расширенный функционал**
- Drag & Drop категорий
- Массовое редактирование операций
- Импорт данных (CSV, банковские выписки)

**Промт 6.4: Резервное копирование**
- Автоматический бэкап
- Восстановление из бэкапа
- Облачная синхронизация (опционально)

---

## Текущая структура проекта
```
AutoKassa/
├── .claude-context.md          # Этот файл
├── AutoKassa.csproj
├── App.xaml / App.xaml.cs
├── MainWindow.xaml / MainWindow.xaml.cs
│
├── Data/
│   ├── AppDbContext.cs
│   └── Migrations/
│
├── Models/
│   ├── Transaction.cs
│   ├── Category.cs
│   ├── Settings.cs
│   ├── Enums/
│   │   └── OperationType.cs
│   └── Reports/
│       ├── BalanceReport.cs
│       └── (другие отчеты...)
│
├── Services/
│   ├── ITransactionService.cs / TransactionService.cs
│   ├── ICategoryService.cs / CategoryService.cs
│   ├── IReportService.cs / ReportService.cs
│   ├── IPasswordService.cs / PasswordService.cs
│   ├── IBackupService.cs / BackupService.cs
│   ├── ISettingsService.cs / SettingsService.cs
│   ├── IDialogService.cs / DialogService.cs
│   ├── INavigationService.cs / NavigationService.cs
│   └── ILockService.cs / LockService.cs
│
├── ViewModels/
│   ├── ViewModelBase.cs
│   ├── MainWindowViewModel.cs
│   ├── InitialSetupViewModel.cs
│   ├── TransactionsViewModel.cs
│   ├── TransactionEditViewModel.cs
│   ├── CategoriesViewModel.cs
│   ├── CategoryEditViewModel.cs
│   ├── ReportsViewModel.cs
│   └── Reports/
│       ├── BaseReportViewModel.cs
│       └── BalanceReportViewModel.cs
│
├── Views/
│   ├── InitialSetupView.xaml / .xaml.cs
│   ├── LockScreenView.xaml / .xaml.cs
│   ├── TransactionsView.xaml / .xaml.cs
│   ├── TransactionEditView.xaml / .xaml.cs
│   ├── CategoriesView.xaml / .xaml.cs
│   ├── CategoryEditView.xaml / .xaml.cs
│   ├── ReportsView.xaml / .xaml.cs
│   └── Reports/
│       └── BalanceReportView.xaml / .xaml.cs
│
├── Helpers/
│   ├── RelayCommand.cs
│   └── Converters/
│       ├── BoolToVisibilityConverter.cs
│       ├── InverseBoolConverter.cs
│       ├── DecimalToStringConverter.cs
│       ├── OperationTypeToColorConverter.cs
│       └── OperationTypeToTextConverter.cs
│
└── Styles/
    ├── Colors.xaml
    └── Buttons.xaml
```

---

## Архитектурные решения

### MVVM Pattern
- **Model** - сущности БД (Transaction, Category)
- **ViewModel** - бизнес-логика, команды (TransactionsViewModel)
- **View** - XAML разметка (TransactionsView.xaml)
- Связь через DataBinding и ICommand

### Dependency Injection
Все сервисы регистрируются в `App.xaml.cs`:
```csharp
services.AddScoped<ITransactionService, TransactionService>();
services.AddScoped<ICategoryService, CategoryService>();
services.AddScoped<IReportService, ReportService>();
// ...
```

### Навигация
Через `INavigationService` в `MainWindowViewModel`:
```csharp
_navigationService.NavigateTo<TransactionsViewModel>();
```

### Команды
Через `RelayCommand`:
```csharp
AddCommand = new RelayCommand(_ => AddTransaction());
```

---

## Стандарты кодирования

### Именование
- **PascalCase** - классы, методы, свойства, константы
- **camelCase** - параметры методов, локальные переменные
- **_camelCase** - private поля класса
- **Async** суффикс - для асинхронных методов

### Комментарии
- XML комментарии для public API
- `/// <summary>` для описания
- Inline комментарии для сложной логики

### Async/Await
Все операции с БД - асинхронные:
```csharp
public async Task<List<Transaction>> GetAllAsync()
{
    return await _context.Transactions.ToListAsync();
}
```

---

## База данных

### Провайдер
SQLite - файл `autokassa.db` в папке приложения

### Миграции
```bash
dotnet ef migrations add MigrationName
dotnet ef database update
```

### Seed данные
Категории по умолчанию добавляются при первом запуске.

---

## Текущий статус

### ✅ Реализовано
- Инициализация проекта
- База данных (EF Core + SQLite)
- Начальная настройка
- Блокировка экрана
- Управление операциями (CRUD)
- Управление категориями (CRUD)
- Структура отчетов
- Отчет "Баланс за период" с графиком OxyPlot

### 📋 Планируется
- Другие отчеты
- Экспорт отчетов (PDF/Excel)
- Система бюджетов
- Аналитика
- Темная тема
- Улучшения UI/UX

---

## Известные проблемы

- Нет известных проблем
---

## Для Claude Code

При работе с проектом:
1. Следуй архитектуре MVVM
2. Используй существующие сервисы через DI
3. Добавляй XML комментарии к public API
4. Соблюдай стиль кодирования
5. Тестируй изменения перед commit
6. Обновляй этот файл при добавлении нового функционала

---

## Полезные команды

### Сборка
```bash
dotnet build
dotnet run
```

### Миграции
```bash
dotnet ef migrations add MigrationName
dotnet ef database update
```

### NuGet
```bash
dotnet add package PackageName
dotnet restore
```

---

**Последнее обновление:** Этап 3.3 завершен (добавлен график OxyPlot в отчет баланса)
**Следующий шаг:** Промт 3.4 - отчет "Структура по категориям"